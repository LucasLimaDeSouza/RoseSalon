import styled from "styled-components";

export const BoxGallery = styled.main`
    padding-bottom: 6rem;
    display: flex;
    
    flex-wrap: wrap;
    gap: 5rem;
    justify-content: center;
    margin-top: 9.4rem;
`